static const int screen_width = 720;  
static const int screen_height = 640; 
static const float scale = 1.0f;      

static const float half_width = float(screen_width) / 2.0f;
static const float half_height = float(screen_height) / 2.0f;

static const float screen_top = half_height;
static const float screen_bottom = -half_height;
static const float screen_left = -half_width;
static const float screen_right = half_width;

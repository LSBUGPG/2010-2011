static const int screen_width = 640;  // in pixels
static const int screen_height = 480; // in pixels
static const float half_width = float(screen_width / 2);
static const float half_height = float(screen_height / 2);
